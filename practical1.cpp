#include <iostream>  // Includes the standard input/output stream library

using namespace std; // Allows direct use of standard library names like cout without std:: prefix

int main() {         // Entry point of the program (use int main() instead of void main())
    cout << "Hello, world!"; // Prints output to the screen using the standard output stream

    return 0;        // Returns 0 to indicate successful execution
}
